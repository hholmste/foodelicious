http:
  port: 8080
  rootPath: /foodelicious/*

# Database settings.
mongoConfiguration:
  # the JDBC URL
  url: localhost
  port: 27017


logging:

  loggers:
    "com.sun.jersey.api.container.filter.ØoggingFilter": INFO
  console:
    timeZone: GMT+01